import { COLOR } from 'styles/colors';
import { ICON_FILTER } from 'styles/grayscale';
import { MEDIA } from 'styles/media';
import { UTILS } from 'styles/utils';

const theme = { COLOR, MEDIA, ICON_FILTER, UTILS };
export default theme;
