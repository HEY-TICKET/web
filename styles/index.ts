import { COLOR as color } from 'styles/colors';
import { ICON_FILTER as iconFilter } from 'styles/grayscale';
import { MEDIA as media } from 'styles/media';
import { UTILS as utils } from 'styles/utils';

const STYLES = { color, media, iconFilter, utils };

export default STYLES;
